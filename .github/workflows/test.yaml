name: Test

on:
  push:

jobs:
  docker:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Run tests
        run: pip3 install . && python3 -m pytest
